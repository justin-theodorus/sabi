# TEMPLATE ONLY — do not commit real values.
# Populate with: kubectl create secret generic sabi-secrets -n sabi \
#   --from-literal=ANTHROPIC_API_KEY=sk-... \
#   --from-literal=ELEVENLABS_API_KEY=... \
#   --from-literal=SUPABASE_URL=https://... \
#   --from-literal=SUPABASE_PUBLISHABLE_KEY=... \
#   --from-literal=SUPABASE_SECRET_KEY=... \
#   --from-literal=MINIO_ACCESS_KEY=minioadmin \
#   --from-literal=MINIO_SECRET_KEY=minioadmin
apiVersion: v1
kind: Secret
metadata:
  name: sabi-secrets
  namespace: sabi
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
  ELEVENLABS_API_KEY: "REPLACE_ME"
  SUPABASE_URL: "REPLACE_ME"
  SUPABASE_PUBLISHABLE_KEY: "REPLACE_ME"
  SUPABASE_SECRET_KEY: "REPLACE_ME"
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
