# Default Locust config — override any flag on the CLI
host = http://localhost:8000
headless = false
web-port = 8089

# Recommended profiles — run as:
#   locust --config tests/locust/locust.conf --tags expression
#   locust --config tests/locust/locust.conf --tags dialogue
#   locust --config tests/locust/locust.conf --tags full-session

# Expression HPA trigger test
# locust -f tests/locust/locustfile.py ExpressionStressUser \
#   --users 80 --spawn-rate 1 --run-time 3m

# Dialogue rate-limit test
# locust -f tests/locust/locustfile.py DialogueStressUser \
#   --users 60 --spawn-rate 1 --run-time 2m

# Realistic combined load
# locust -f tests/locust/locustfile.py FullSessionUser \
#   --users 20 --spawn-rate 2 --run-time 5m
